import { EnvConfig } from 'src/app/shared/models/env-config';
import { Environment } from 'src/app/shared/models/environment.enum';

export const envConfig: EnvConfig = {
  env: Environment.development,
};
