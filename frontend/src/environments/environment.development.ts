import { EnvConfig } from 'src/app/shared/_models/env-config';
import { Environment } from 'src/app/shared/_models/environment.enum';

export const envConfig: EnvConfig = {
  env: Environment.development,
};
